name: simple_code_lesson_2
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2

  flutter_localizations:
    sdk: flutter
  intl: ^0.17.0
  flutter_svg: ^1.1.0
  flutter_bloc: ^8.0.1
  shared_preferences: ^2.0.15
  http: ^0.13.4
  dio: ^4.0.6
  json_annotation: ^4.5.0
  

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  build_runner: ^2.1.11
  json_serializable: ^6.2.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/svg/
    - assets/images/bitmap/


flutter_intl:
  enabled: true
